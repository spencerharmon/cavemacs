#+TITLE: cavemacs — Implementation Plan
#+AUTHOR: Spencer Harmon
#+STARTUP: overview
#+OPTIONS: toc:2 num:nil

* Status: v0.0.15 — prose body kill/yank strips `│ ' rule prefix; tool-card box rules already stripped (v0.0.14)

All milestones M0–M8 complete plus M10 (slash command dispatch +
in-prompt autocomplete).  M9 stretch deferred.  See =CHANGELOG.md=.

Two interactive-only regressions discovered after the initial cut --
R1 (upside-down layout) and R2 (RET stops sending after first prompt)
-- are both fixed; see the postmortem below.

The notes below preserve the original plan for historical reference.
Material corrections discovered during execution are flagged inline
with =[CORRECTED]= markers.

* Performance: long-conversation CPU-bound rendering

=cavemacs= becomes CPU-bound when the conversation grows long
and/or the current assistant message gets large.  Root cause is
that =cavemacs-render--repaint-assistant= rebuilds and
re-fontifies the *entire current assistant message* on every
streaming =text_delta= event:

- =(concat cur delta)= on overlay prop → O(N) string copy per
  delta → O(N²) per message.
- =delete-region= then re-insert all text.
- =cavemacs-render--fontify-markdown-string= spins up a
  =with-temp-buffer=, runs =markdown-mode= + =font-lock-ensure=
  over the *full accumulated text* every delta. Dominant cost.
- =cavemacs-render--decorate-code-blocks= rescans the body and
  makes fresh overlays each repaint.
- =cavemacs-render--apply-variable-pitch= creates a new overlay
  each repaint.

Secondary scaling: per-message + per-tool + per-collapse-body +
per-code-block overlays accumulate unbounded across turns.  Emacs
redisplay walks overlay lists per window update, so very long
sessions slow down even when streaming is idle.

** Improvement backlog (tiers by ROI)

*** Tier 1 — ship first (biggest win / smallest diff)

- [ ] #1  Defer markdown fontify until =text_end= / =message_end=.
      Insert raw text during streaming; fontify once when the
      message is complete.
- [ ] #3  Coalesce streaming deltas via a short idle timer
      (~30 ms).  Update overlay text immediately, repaint once
      per idle tick instead of per delta.  =text_end= flushes
      synchronously.
- [ ] #11 Bind =inhibit-modification-hooks t= and
      =buffer-undo-list t= around every render insert.  Stops
      foreign after-change hooks and undo-list growth.
- [ ] #12 Raise =gc-cons-threshold= during a streaming turn,
      restore on =turn_end= / =agent_end=.  Reduces mid-stream
      GC pauses from string churn.
- [ ] #25 Never re-fontify finalized prior messages.  Once a
      message has hit =message_end=, no later event touches its
      overlay body.
- [ ] #26 Profile before further tuning: =M-x profiler-start=
      cpu, replay a long stream, =profiler-report=.  Confirm
      hotspot is still markdown fontify and not something
      surprising (=display-line-numbers-mode=, =hl-line=,
      =lsp-mode=, etc.).

*** Tier 2 — follow-up

- [ ] #2  Incremental append instead of full-body repaint:
      track last-inserted length on the overlay, insert only the
      new suffix on each delta.
- [ ] #4 / #17  Convert finalized turns from overlays to plain
      text properties.  Text-properties are tree-indexed; Emacs
      redisplay's overlay scan is O(overlays × windows ×
      redisplay-events).
- [ ] #5  Reuse a single =--apply-variable-pitch= overlay per
      message; extend its end-marker on append.
- [ ] #6  Use =jit-lock= deferred fontify for visible region
      only, instead of synchronous =font-lock-ensure=.
- [ ] #7  Skip fontify entirely for off-screen messages: bail if
      the overlay does not intersect =(window-start)= ..
      =(window-end)=.
- [ ] #8  Replace =with-temp-buffer markdown-mode= round-trip
      with a hand-rolled regex pass for bold / italic / inline
      code / fenced code.  10–50× faster.
- [ ] #9  =inhibit-point-motion-hooks= around repaint inserts.
- [ ] #13 Use =(insert delta)= against the overlay's end rather
      than building =(concat cur delta)= in the overlay prop.
- [ ] #14 Reuse a single hidden scratch buffer for fontify
      instead of =with-temp-buffer= (which allocates + tears
      down each call).
- [ ] #15 Cap decoration overlays per message.
- [ ] #16 Markers with =rear-advance= on a single per-message
      overlay; insert appends and the overlay end auto-extends.
- [ ] #18 Avoid =json-parse-string= over huge tool outputs;
      stream-parse or hard-cap the parsed slice.  Confirm
      collapsed bodies skip parse too.
- [ ] #19 Audit any remaining =split-string= on large payloads.
- [ ] #20 Coalesce multiple events received in a single filter
      chunk: repaint once after the whole chunk drains.
- [ ] #21 Cache the input-area boundary marker for RET; avoid
      buffer scans on each keystroke.
- [ ] #22 Guard =recenter -1= behind a "was at bottom" check
      (don't snap-back when the user has scrolled up to read
      history).
- [ ] Disable =display-line-numbers-mode= /
      =global-display-line-numbers-mode= in
      =cavemacs-shell-mode=: line-number redisplay is
      O(buffer-size) per refresh.

*** Tier 3 — architectural

- [ ] #23 Virtualize old turns: collapse to a one-line summary
      overlay; expand on click.
- [ ] #24 Hard cap visible history: keep last N turns rendered,
      replace earlier ones with a "[… K earlier turns]"
      placeholder; re-render on demand.

** v0.0.32 ships Tier 1 (#1 + #3 + #11 + #12 + #25 + the
   line-numbers guard from Tier 2 because it's a one-liner with
   a known-large impact).

* Postmortem: R3 (silent upstream errors)

Symptom: switching to `claude-opus-4.7` and prompting produced an
empty assistant block.  Modeline went back to idle.  Nothing in
=*Messages*=.  No indication anything had failed.

Root cause: caveman emits a `message_end' event with
`stopReason: "error"' and an `errorMessage' string field when a
provider rejects the request.  cavemacs-render's
`cavemacs-render--on-message-end' was ignoring both fields,
treating the message as a normal (empty) completion.

In this specific case Anthropic via Vertex/Copilot returned:
  =400 invalid_request_error: "thinking.type.enabled" is not
   supported for this model. Use "thinking.type.adaptive" and
   "output_config.effort" to control thinking behavior.=

The same pattern would hit any provider rejection -- rate limits,
content filters, malformed bodies, model removed from the
provider's catalog, etc.

Fix: detect `stopReason="error"' in `on-message-end', extract
`error.message' via `json-parse-string' from the trailing JSON,
prepend a status code if present, and paint the result with the
error face inside the assistant overlay.  Falls back to "⚠ <raw>"
when the message doesn't parse as JSON.

Not fixed (upstream issue): caveman defaults to
`thinking.type=enabled', which newer Anthropic models reject.
A workaround is to set thinking off before using those models
(=M-x cavemacs-flags= → Set thinking level → off).

* Postmortem: R1 + R2 (interactive-only regressions)** R1 — UI appeared upside-down

Symptom: in interactive Emacs the buffer showed the banner at the top
of the window and the prompt scrolled off the bottom.  Comparable
interfaces (caveman's TUI, browser chat, every Emacs comint-derived
shell) put the prompt at the bottom of the visible window with the
most recent message just above it.

Root cause: the buffer's *content* order was already correct (oldest
turn first, newest turn just above the separator+prompt).  But the
window's default scroll position was BOB, so the prompt was off the
bottom of the visible area.  The interactive tests caught it, the
batch tests (which never displayed the buffer in a window) did not.

Fix: in `cavemacs-shell-mode' set the standard comint scroll settings
(`scroll-conservatively' 101, `scroll-margin' 0).  And in
`cavemacs-render--at-output' detect when point is at point-max
*before* a render insert and advance it (plus every live window's
window-point) to the new point-max *after* the insert.  Result:
typing input area stays pinned to the bottom of the window; new
turns render just above it; older turns scroll up out of view.

** R2 — RET stopped sending after the first prompt

Symptom: after the first response rendered, hitting RET in the input
area inserted a literal newline instead of submitting.  Could only
send the very first prompt.

Root cause: `save-excursion' in `cavemacs-render--at-output' restores
point by its *numeric position*, not by trailing-the-tail.  When the
user's point was at point-max during a streaming render, save-excursion
snapshotted the numeric position; the renderer then inserted text
*before* that position; after restore, point was now in the middle of
the rendered output, *not* at point-max.  `(eobp)' returned nil, and
`cavemacs-shell-send-or-newline' fell through to its `(newline)' branch.

Why batch tests missed it: every batch test explicitly did
`(goto-char (point-max))' before each `cavemacs-shell-send', which
papered over the issue.  The stricter regression test added with the
fix uses `(call-interactively #'cavemacs-shell-send-or-newline)' and
asserts `(eobp)' *without* a manual goto-char first.

Fix: same as R1 (advance point + every window-point to the new
point-max after each render insert, when the user was at the bottom).

* M14 — `C-c C-c' shell control: abort if busy, abandon prompt if idle

** Status

Shipped v0.0.16. `cavemacs-shell-abort' is context-sensitive:
- Busy: prompt via `cavemacs-shell--confirm-abort' (indirection
  exists so tests can stub it without redefining `yes-or-no-p',
  which triggers native-comp trampoline generation in batch).
  On confirm, sends `abort' RPC.
- Idle: walks point to point-max, ensures newline, calls
  `cavemacs-shell--install-prompt' again to drop a fresh
  separator + prefix + markers, moves point and every live
  window-point to the new input area. Abandoned text stays in
  the buffer between the OLD prompt prefix and the NEW
  separator -- not sent, still copy-pasteable.
Regression tests: `c-c-c-idle-abandons-line',
`c-c-c-busy-aborts-on-confirm', `c-c-c-busy-declined-noop'.

** Goals

1. *Busy path.* When the agent is mid-turn (any in-flight
   prompt / tool execution / streaming response), pressing
   =C-c C-c= in the shell buffer prompts in the minibuffer
   (=y-or-n-p= / =yes-or-no-p=, TBD) whether to abort. On
   confirmation, send the caveman =abort= RPC verb and let the
   normal =turn_end= / =agent_end= path clean up. On decline,
   no-op.
2. *Idle path.* When the agent is NOT busy, =C-c C-c= abandons
   the current shell input line *in place* (the typed text is
   left in the buffer as part of the scrollback, NOT sent to
   the agent), emits a fresh separator + prompt at
   =point-max=, repositions =cavemacs-shell--prompt-marker= and
   =cavemacs-shell--input-start-marker= to the new prompt, and
   moves point (and every live window-point displaying the
   buffer) to the new input start.

   Rationale: lets the user back out of a half-written prompt
   without manually selecting and deleting it. Mirrors the
   common bash/zsh =C-c= reflex ("forget this line, give me a
   fresh one"). Keeping the abandoned text visible preserves
   the user's context for copy-paste rather than discarding
   it.

** Implementation sketch

- New command =cavemacs-shell-cancel-or-reset=. Dispatch on
  =cavemacs-shell--busy-p= (derive from
  =cavemacs-render--current-assistant-ov= /
  =cavemacs-render--busy= -- pick one canonical source; see M15
  below for cleanup).
- Busy branch: =(when (yes-or-no-p "Abort in-flight turn? ")
  (cavemacs-rpc-send `((type . "abort"))))=.
- Idle branch:
  - Strip the read-only / =field= properties from the current
    input line so the abandoned text behaves like normal
    scrollback (still visible, still selectable, no longer the
    live prompt).
  - Move to =point-max=, =insert= a newline if needed.
  - Call the same prompt-rendering helper used by
    =cavemacs-shell--insert-prompt= (factor it out if it's not
    already a standalone function).
  - Update =prompt-marker= / =input-start-marker= to the new
    locations.
  - =goto-char input-start-marker=; mirror on every window
    displaying the buffer.
- Bind in =cavemacs-shell-mode-map=: =C-c C-c=.

** Tests to add

- =cavemacs-shell/c-c-c-idle-abandons-line=: type input, force
  busy=nil, call command, assert (a) abandoned text still in
  buffer, (b) new prompt prefix appears below, (c) point is at
  the new input-start-marker, (d) the old text is NOT sent
  (mock =cavemacs-rpc-send= and assert no =prompt= verb fired).
- =cavemacs-shell/c-c-c-busy-aborts=: force busy=t, stub
  =yes-or-no-p= → t, assert an =abort= RPC verb was sent.
- =cavemacs-shell/c-c-c-busy-declined-noop=: stub →nil, assert
  no RPC sent and the input area is untouched.

** Related files

- =cavemacs-shell.el= (new command + binding; possibly factor
  out the prompt-render helper)
- =cavemacs-rpc.el= (no change; =abort= verb already exists per
  AGENTS.md §5)
- =tests/cavemacs-commands-test.el= or a new
  =tests/cavemacs-shell-test.el=

* Bug: header-line busy indicator never clears

** Symptom

The pretty header-line's busy/idle indicator (top of the shell
buffer) is stuck on "busy" even when the agent is fully idle
(no streaming response, no in-flight tool, prompt is editable,
RET sends fine). It updates *into* the busy state at turn
start but never updates back to idle on =turn_end= /
=agent_end=.

** Suspected cause

One of:
- =cavemacs-render--on-turn-end= / =--on-agent-end= isn't
  clearing the busy flag (or is clearing a different flag
  than the header-line reads).
- Header-line is computed from one source
  (e.g. =cavemacs-render--current-assistant-ov=) but the
  termination handler clears another (a separate
  =cavemacs--busy= var) and the two have drifted apart.
- =force-mode-line-update= isn't being called on the right
  buffer after the state flips, so the displayed string is
  stale.

Needs a single-source-of-truth audit: pick ONE canonical busy
predicate, route every state transition through it, and have
the header-line + =C-c C-c= dispatcher + any other consumer
read from that one source.

** Task

Ensure ALL status fields shown in the header-line update
dynamically and correctly across the full session lifecycle:
agent_start / turn_start / message_start / tool_execution_*
/ turn_end / agent_end / abort / error. Add a regression test
that drives the synthetic event sequence and asserts the
busy indicator (and any other status field -- model, token
count, cost) reads the expected value after each phase.

** Related files

- =cavemacs-pretty.el= (header-line formatter)
- =cavemacs-render.el= (busy-flag transitions on lifecycle
  events)
- =cavemacs-shell.el= (consumes the busy flag for input
  affordances; will also consume it for M14)
- =tests/cavemacs-render-test.el= (lifecycle-state assertions)

* Bugfix: C-a moves point into read-only prompt

** Symptom

Pressing =C-a= (=beginning-of-line= / =move-beginning-of-line=) in
the shell input area moves point to column 0, inside the read-only
prompt prefix (the => = character and separator).  The user can't
type there, but the cursor position is confusing and breaks
expectations — =C-a= should land at the first editable column.

** Fix

Bind =C-a= (and possibly =home=) in =cavemacs-shell-mode-map= to a
command that moves point to =cavemacs-shell--input-start-marker=
instead of =(line-beginning-position)=.  Respect =shift-select-mode=
so =C-S-a= still sets mark correctly.

** Related files

- =cavemacs-shell.el=

* M13 — UI polish: assistant header scoping + full tool args on expand

** Status

Shipped v0.0.16. Assistant header (now labelled `Caveman') is
deferred until first prose/thinking emission in a message;
tool-only assistant messages render with no header. Tool cards
render their full input arguments above the result, both
included in the same collapsible body. Tests added per spec.
(Tool-only-message detection works by deferring header emit;
if a message ever produces tool calls AFTER prose, the header
emitted on first prose is preserved -- correct.)

** Goals

1. *Scope the assistant header line* (=▌ ✦ Assistant · provider/model · HH:MM=)
   so it precedes ONLY prose and thinking blocks (combined when
   adjacent in the current turn, exactly as today). Tool-call
   cards must render WITHOUT a preceding assistant header line.
   Rationale: the header is meta about an LLM utterance; tool
   calls are mechanical side-effects, not utterances, and the
   repeated header creates visual noise in turns with many tools.
2. *Rename =Assistant= → =Caveman=* in the header label. The product
   identity is caveman; calling it "Assistant" is a leftover from
   generic chat scaffolding.
3. *Show full tool input on expand.* Today the collapsed tool-card
   header shows a truncated single-line arg summary (e.g.
   =edit · path=foo.el …=). When the card is expanded, the BODY
   shows only the tool *result* — the full input (edit oldText /
   newText, full bash command string, full file path, etc.) is
   never visible. Expanding should reveal the full input ABOVE the
   result, with the collapsed view still showing the same
   truncated summary in the header line (the header is the
   identity; the body is the detail).

** Implementation sketch

- =cavemacs-render--on-message-start= currently emits the
  assistant header unconditionally at message start. Defer the
  header emit until the first prose / thinking insertion within
  the message; suppress it for messages whose only content was
  tool calls. Track "header already emitted for this message"
  via a buffer-local plist keyed by message id.
- Label change: single string constant swap inside the header
  formatter (search for "Assistant" in =cavemacs-render.el= /
  =cavemacs-pretty.el=).
- Tool full-input on expand: in =cavemacs-render--on-tool-start=,
  after writing the header line, insert a pretty-printed full-arg
  block (each key on its own line, multi-line values indented
  under their key) with the same =│ = rule prefix used by the
  result body. Register this region as part of the collapsible
  body so it's hidden in the default collapsed state and revealed
  on TAB/RET. =--on-tool-end= then appends the result block below
  it (separator rule between input and output for readability).

** Tests to add

- =cavemacs-render/tool-call-has-no-assistant-header=:
  message_start (role=assistant) → tool_execution_start →
  tool_execution_end → message_end (no text content). Assert the
  buffer does NOT contain the header glyph / "Caveman" label.
- =cavemacs-render/assistant-header-emitted-once-per-prose-message=:
  message with mixed thinking + prose still produces exactly one
  header line.
- =cavemacs-render/tool-expand-shows-full-args=: tool with a
  multi-line =command= arg renders the full command inside the
  expanded body (use buffer-substring with the collapse overlay
  removed). Truncated summary still appears in the header.
- =cavemacs-render/header-label-is-caveman=: assertion on the
  literal label string.

** Related files

- =cavemacs-render.el= (message_start header gating; tool-start
  full-arg block insertion; collapse region extension)
- =cavemacs-pretty.el= (header label constant; possibly the
  glyph)
- =tests/cavemacs-render-test.el=

** Out of scope

- Restructuring tool cards to not use a box at all.
- Changing the truncated summary format itself.
- Per-tool custom expanded layouts (edit-diff styling, bash
  syntax highlighting on the command line, etc.) — nice-to-have
  follow-ups.

* M12 — Worker dispatch (deferred)

** Goal

UI parity with caveman's TUI: recognise an =& prompt= prefix in
the input area and dispatch to a registered caveman worker (a
background subagent with its own context window, sometimes its
own model and tool set).  Worker results stream back into the
main session as a marked turn.

** Status

Deferred.  User has not yet used caveman workers and wants to
explore the underlying caveman-code feature first before
committing to a UX shape on the cavemacs side.

** Pre-implementation questions

Before this is in scope:

1. Does caveman --mode rpc actually expose workers?  If so, what
   are the RPC verbs (=spawn_worker= / =worker_prompt= /
   =worker_status= / ...)?  If workers are strictly a TUI
   input-layer feature, this milestone requires an upstream
   change first.
2. What's the user's mental model -- fire-and-forget background
   jobs, parallel exploration, dedicated long-running
   investigators?  The UX changes substantially across those.
3. How visible should they be -- minimal prefix interception in
   the main buffer, or a sibling worker buffer / status section
   on the pretty header-line?

** Related files (when implemented)

- =cavemacs-shell.el= (input area `&' prefix interception)
- =cavemacs-render.el= (=[worker:NAME]= turn marker rendering)
- =cavemacs-rpc.el= (worker verbs)
- =cavemacs-pretty.el= (header-line worker count, optional)

* M11 — Collapsible thinking text and tool-call blocks

** Status

In progress, shipping iteratively as v0.0.X patch releases. As of
v0.0.12 thinking and tool blocks both render collapsed-by-default
with TAB/RET toggle; chasing residual rendering bugs from real
caveman event shapes.

** Bugs fixed during M11 implementation (chronological)

Each was discovered only after real-LLM session testing; unit
tests passed throughout but didn't exercise the relevant shape.

- R-M11.1 (=00fc53c=, v0.0.x line begins): collapse body overlays
  created with =rear-advance=t= grew with every subsequent insertion,
  swallowing later assistant prose into the invisible region.
  Fix: =rear-advance=nil=.
- R-M11.2 (=a3e0131=): TAB/RET in shell buffer were bound
  buffer-wide to =cavemacs-shell-complete= / =-send-or-newline=,
  shadowing the text-property keymap installed on collapse headers.
  Fix: both commands now check for =cavemacs-collapse-id= at point
  and dispatch to =cavemacs-render-toggle-at-point= first.
- R-M11.3 (=acd7f24=): tool-card header positions were stored as
  raw integers in overlay props. Intervening insertions made the
  integers stale; =register-block= tagged the wrong region. Fix:
  store as markers.
- R-M11.4 (=4c6a840=): =cavemacs-render-tool-default-collapsed=
  was nil; flipped to t.
- R-M11.5 (=307e3e4=): assistant/user message overlays were
  created with =rear-advance=t=. A tool card inserted at the
  prompt marker (same position as the overlay end) ended up
  *inside* the assistant overlay; the next =message_update= repaint
  ran =(delete-region body-start ov-end)= and wiped the tool card,
  including its =cavemacs-collapse-id= property. Fix: assistant
  + user overlays use =rear-advance=nil=.
- R-M11.6 (=e131446=): tool =cavemacs-header-end= marker was
  rear-advancing. During tool execution every subsequent insertion
  pushed it forward by hundreds of chars; by =--on-tool-end= time,
  =register-block= tagged a 400-char swath that didn't include the
  header line. Fix: non-advancing marker.
- R-M11.7 (=b60e8bd=): =cavemacs-render--render-tool-result=
  didn't understand caveman's content-parts shape
  (=[((type . "text") (text . "..."))]=); threw stringp error,
  aborting =--on-tool-end= before =register-block= ran. Tools were
  stuck status=running with no collapse property. Fix: add
  content-parts branch (and tighten alist detection so plain
  =((output . "z"))= doesn't false-positive into it).
- R-M11.8 (=ccd53c0=): caveman emits tool output BOTH as a
  =tool_execution_end= event (rendered in the tool card) AND as a
  =message_start= with role=toolResult containing the same text.
  The default 'unknown role' branch rendered the latter as
  =[toolResult]= + raw text, duplicating the body. Fix: explicit
  pcase arms for =tool=/=toolResult=/=tool_result= that no-op.
- R-M11.9 (=e22836c=): collapsed body overlay's =before-string=
  summary contained a newline. Emacs treated it as a virtual
  visual line; C-p past the close-box rule got stuck. Fix: drop
  the summary string.
- R-M11.10 (=930f3d4=): same root cause as R-M11.9 but for thinking
  blocks (had its own summary string). Dropped.

Lessons:
- =rear-advance= on long-lived overlays is almost always wrong.
- Always store text positions as markers when there's any insertion
  between recording and use.
- Real caveman events don't match the type signatures we assumed
  from =docs/api.md=; trust =rpc-types.ts= and instrument before
  designing handlers.

** Goal

Thinking text and tool-call output can be verbose, dominating the
buffer and pushing conversation content off-screen.  Render these
blocks collapsed by default with a clickable/keybound toggle to
expand or collapse them in place.

** Requirements

- Thinking blocks render collapsed (single summary line, e.g.
  =▶ Thinking (42 lines)=) by default.
- Tool-call blocks render collapsed (e.g. =▶ tool: read file.el=)
  by default.
- =TAB= or =RET= on a collapsed header expands the full content
  inline; same key collapses it again.
- Mouse click on the header also toggles.
- A command / custom variable to toggle *all* blocks in the buffer
  (expand-all / collapse-all).
- Expanded state is buffer-local and non-persistent (restarting
  session resets to collapsed).

** Implementation sketch

- Use overlays with =display= or =invisible= property on the body
  region; keep the header line always visible.
- Store expand/collapse state as an overlay property.
- Add a keymap on the header overlay (=keymap= property) for
  =TAB= / =RET= / mouse-1 toggles.
- Hook into =cavemacs-render.el= where thinking and tool-call
  events are inserted.

** Related files

- =cavemacs-render.el= (insertion of thinking / tool-call blocks)
- =cavemacs-shell.el= (buffer keymap coordination)

* M10 — Slash command dispatch + in-prompt autocomplete

** Problem (discovered post-v0.1.0)

Typing =/help= or =/model= in the input area sent the literal string
as a `prompt' RPC.  The LLM dutifully answered the question in plain
English ("Here are the tools and functionality you can use..."), but
nothing happened mechanically: the model didn't change, the context
didn't compact, the session didn't reset.

Root cause: caveman's built-in slash commands (=/help=, =/model=,
=/compact=, =/cost=, =/clear=, ~30 of them) are implemented in
caveman's *interactive TUI input handler*, not in the agent loop.
In =--mode rpc=, that handler is bypassed entirely.  Sending =/model=
as a `prompt' is treated as user text, not a command.

User-defined slash commands (extensions, prompt templates, skills)
do work over RPC: caveman's `prompt' handler invokes them before
forwarding to the LLM.  But those have to be installed by the user
first; the built-ins were the missing piece.

** Design

Two pieces:

*** Local dispatch

A small built-in registry in =cavemacs-commands.el= maps the most
important caveman built-ins to their direct RPC equivalents:

| Slash    | RPC verb              |
|----------+-----------------------|
| /help    | (local, prints help)  |
| /model   | get_available_models + set_model |
| /thinking | set_thinking_level   |
| /compact  | compact              |
| /new, /clear | new_session       |
| /name    | set_session_name      |
| /session | get_session_stats     |
| /export  | export_html           |
| /copy    | get_last_assistant_text |
| /bash    | bash                  |
| /abort   | abort                 |

`cavemacs-shell-send' calls `cavemacs-commands-dispatch' before
sending; if the input parses as a known built-in, the handler runs
and the input is *not* forwarded to the LLM.  Otherwise (regular
prompts and user-defined slash commands alike), the text goes
through to caveman's `prompt' RPC unchanged.

*** In-prompt autocomplete via completion-at-point

Adds `cavemacs-commands-capf' to the buffer's CAPF list.  Active
when the input area starts with =/= and point is on the first word.
Offers both built-in commands and user-defined commands (the latter
fetched lazily via the `get_commands' RPC, cached for 5 seconds per
buffer to avoid round-trip storms during typing).

Works automatically with the default Emacs completion UI, Corfu,
Company, and any other CAPF consumer.  =TAB= is bound to
`completion-at-point' in cavemacs-shell-mode for users without a
popup completion frontend.

** Exit criteria (met)

- =/help= in the input area shows the built-in command list inline.
- =/model RET= opens a completing-read picker and switches models.
- =/compact RET= triggers a real context compaction.
- Typing =/= in an otherwise-empty input area + TAB shows
  completion candidates with descriptions.
- User-defined commands from =get_commands= appear in the
  completion list alongside built-ins (when an extension or prompt
  template is installed).
- Regression tests cover the parser, dispatch decision, and CAPF
  shape.

* Overview

cavemacs is an Emacs front-end ("shell") for the [[https://github.com/JuliusBrussee/caveman-code][caveman-code]]
terminal coding agent. It wraps the =caveman= binary as an external
process and surfaces its agent loop inside an Emacs buffer with
streaming output, markdown rendering, tool call approvals, and
per-project session persistence. It is designed to compose cleanly
with the rest of the Caveman ecosystem:

- *caveman-code* — the agent runtime cavemacs drives (source of truth).
- *cavekit* — the spec-driven workflow (=/ck:spec=, =/ck:build=,
  =/ck:check=) lives as slash commands inside the caveman session;
  cavemacs surfaces them in completion + a transient menu.
- *cavemem* — persistent memory backend; cavemacs does not talk to it
  directly (caveman-code already does), but exposes =/memory= commands
  and a viewer-launch helper.

** Target environment

- Emacs 30.2+ (per user).
- =caveman= installed on =PATH= (npm: =@juliusbrussee/caveman-code=).
- Optional: =cavemem= installed; viewer at http://127.0.0.1:37777.
- Optional: =cavekit= plugin installed in the project (=SPEC.md=,
  =.claude/= or =.cave/= dirs).

** Protocol decision (key finding from research)

caveman-code exposes four programmatic surfaces (see
=caveman-code/docs/api.md=):

1. =caveman --mode rpc= — *JSON-RPC over stdin/stdout, JSONL framed.*
   Methods: =session.create=, =session.prompt=, =session.events=
   (server-streamed), =session.tool.allow=, =session.compact=,
   =session.fork=, =session.close=.
2. =caveman --mode json -p "…"= — one-shot, stable event stream
   (=session.start=, =tool.call=, =tool.result=, =token=, =session.end=).
3. =caveman serve= daemon (HTTP + WebSocket) + =@juliusbrussee/caveman-sdk=.
4. In-process Node SDK (not usable from Emacs).

*Decision:* cavemacs targets surface #1 (=--mode rpc=) as the primary
transport, with surface #2 as a fallback for one-shot =M-x= helpers.
The daemon (#3) is a *Phase 4 stretch* — useful for multi-client /
SSH-survival but not required for v1.

[CORRECTED during M0 — the docs misrepresent the protocol]

The actual =--mode rpc= protocol verified against
=caveman-code/packages/coding-agent/src/modes/rpc/= is *NOT* JSON-RPC
2.0.  It is a custom envelope:

  Commands  : ={"id"?: <str>, "type": <verb>, ...}=
  Responses : ={"id"?, "type":"response", "command":<verb>, "success":<bool>,
                "data"?: ..., "error"?: ...}=
  Events    : ={"type": <event-type>, ...}=  (no id, no envelope)
  UI reqs   : ={"type":"extension_ui_request", "id":<uuid>, "method": ...}=
  UI replies: ={"type":"extension_ui_response", "id":<echo>, ...}=

Verbs are =prompt= (not =session.prompt=), =new_session= (not
=session.create=), =abort=, =fork=, =compact=, =get_state=,
=get_messages=, =get_commands=, =set_model=, =cycle_model=,
=get_available_models=, =set_thinking_level=, =cycle_thinking_level=,
=switch_session=, =bash=, etc.

There is *no per-command sessionId*.  The running process *is* the
session.  =new_session= resets in place; =switch_session= loads a
different on-disk session file.

Tool approvals do not have a dedicated event type — they flow through
=extension_ui_request= with =method:"confirm"=.

This means the M3 "tool approval UI" design pivoted: rather than a
dedicated approval channel, cavemacs registers a *UI handler* that
claims =extension_ui_request= confirms.  The =-protocol-version=v1=
flag advertised in docs does not exist in this version of caveman.

** High-level architecture

#+begin_src
+-----------------------------------------------------------+
|  Emacs                                                    |
|                                                           |
|  +------------------+      +---------------------------+  |
|  |  cavemacs-shell  |<---->|  cavemacs-rpc             |  |
|  |  (shell-maker UI)|      |  (JSONL framing, dispatch)|  |
|  +--------+---------+      +-------------+-------------+  |
|           |                              |                |
|           v                              v                |
|  +------------------+      +---------------------------+  |
|  | cavemacs-render  |      |  process: caveman --mode  |  |
|  | (md, tool blocks,|      |           rpc             |  |
|  |  diffs, perms)   |      +---------------------------+  |
|  +------------------+                                     |
|                                                           |
|  +------------------+   +---------------------+           |
|  | cavemacs-session |   |  cavemacs-project   |           |
|  | (resume/fork/    |   |  (git root → cwd,   |           |
|  |  browse)         |   |   per-repo sessions)|           |
|  +------------------+   +---------------------+           |
+-----------------------------------------------------------+
#+end_src

* Milestones

** New Milestone — M5.1: Tab-completion of slash commands :ui:
*Goal:* Enable real-time tab-completion for slash commands using Emacs' built-in `completion-at-point` (CAPF).

- Detect =/= at cursor position during input,
  - Populate completions dynamically via CAPF,
  - Auto-fetch descriptions from `~/.cave/commands/*.md`.
- Investigate: get completions directly by asking caveman.
- Leverage existing mechanism from M5 for slash commands table-driven logic.

*Exit criteria:* User typing `/` receives proper completions for commands, their subargs dynamically filtered.

** M0 — Research & spike :setup:
*Goal:* prove the transport works end-to-end from raw Elisp before
investing in UI.

- Pin caveman-code version we'll target (latest stable on npm at
  start of work). Document in =README=.
- Hand-drive =caveman --mode rpc= from a scratch buffer with
  =make-process= + JSONL framing. Send =session.create= →
  =session.prompt= → consume =session.events=. Confirm token deltas,
  tool calls, permission-prompt events, and =session.end= shape.
- Decide on event-stream subscription model: is =session.events= a
  one-shot streaming response per call, or a long-lived subscription?
  (Read =packages/coding-agent/src= in caveman-code to confirm — and
  pin =--protocol-version=v1= per docs/api.md.)
- Capture a recorded transcript fixture; this becomes the basis of
  mock-mode tests later.

*Exit criteria:* a 50-line =scratch.el= that successfully prompts
caveman and prints streamed tokens to =*Messages*=.

** M1 — Package skeleton & RPC core :foundation:
*Goal:* a working but UI-poor =M-x cavemacs= that streams to a buffer.

[CORRECTED] We did *not* use =shell-maker= after all.  After M0
exposed the full event vocabulary, it became clear that:

- Caveman streams asynchronous events outside the prompt/response
  rhythm (=tool_execution_*=, =extension_ui_request=, =compaction_*=).
- Tool approvals need inline interactive blocks at arbitrary buffer
  positions, not at the prompt position.
- Multi-part assistant messages (text + thinking + tool_use
  interleaved) want region-based replacement, not the append-only
  model =shell-maker= assumes.

cavemacs therefore defines its own =cavemacs-shell-mode= derived from
=fundamental-mode=, with a dedicated read-only output area above a
prompt marker and a multi-line input area below.  This kept the
dependency surface small (only =markdown-mode= and =transient=) and
gave full control over overlay-based streaming updates.

*Package layout* (multi-file, one module per concern):

- =cavemacs.el= — entry points, autoloads, top-level customs, version.
- =cavemacs-rpc.el= — process management, JSONL framing, request/
  response correlation, event dispatch.
- =cavemacs-shell.el= — =shell-maker= integration, buffer setup,
  prompt, keymap.
- =cavemacs-render.el= — streaming text insertion, markdown rendering
  (defer to =markdown-mode= fontification for blocks), tool-call
  block formatting.
- =cavemacs-tools.el= — tool-call permission UI (approve/deny/always).
- =cavemacs-session.el= — session metadata, resume, fork, list.
- =cavemacs-project.el= — git-root / =project.el= detection, per-repo
  session directory (=~/.cave/agent/sessions/<cwd-hash>/= mirrors
  caveman's own layout).
- =cavemacs-commands.el= — slash command discovery + completion.
- =cavemacs-cavekit.el= — cavekit transient menu (=/ck:spec= etc.).
- =cavemacs-cavemem.el= — =/memory= helpers + viewer launcher.
- =cavemacs-mock.el= — replay fixture transcripts; used by tests.

*Dependencies* (declare in =Package-Requires=):
- =emacs "30.1"=
- =shell-maker "0.78.1"= (or current)
- =markdown-mode= (for fontification of streamed blocks)
- =transient= (built-in; for cavekit / command menus)
- =project= (built-in)

cavemacs implements caveman's RPC protocol directly, with no
intermediate transport library.  The protocol layer is bespoke to
this project.

*RPC core requirements:*
- One =caveman --mode rpc --protocol-version=v1= subprocess per shell
  buffer; lifecycle tied to buffer.
- JSONL line-oriented reader with partial-line buffering (newline-
  delimited; never assume a single read = single message).
- Request table keyed by =id=; promise-style callbacks (cl-style with
  a small =cavemacs-rpc--pending= hash table).
- Event sink: dispatch on =method= / =type= to per-buffer handlers.
- Graceful shutdown on buffer kill (=session.close= then
  =delete-process=).

*Exit criteria:* =M-x cavemacs= opens a shell, =hello= round-trips,
tokens stream into the buffer in real time, =C-c C-c= cancels the
in-flight prompt.

** M2 — Rendering & markdown :ui:
*Goal:* readable, well-formatted streamed output.

- Streaming insertion that preserves point if user is reading
  scrollback; jump-to-bottom otherwise.
- Per-turn structure: user prompt, assistant response, optional tool-
  call sub-blocks, footer with token/cost from =session.end=.
- Markdown rendering strategy: accumulate the current assistant turn
  in an overlay-backed region; on each token delta, re-fontify the
  changed range using =markdown-mode='s font-lock keywords (cheaper
  than re-rendering). Code fences get =org-src=-style native
  fontification via =markdown-fontify-code-blocks-natively=.
- Caveman-mode terse output renders fine as markdown; no special case.
- Diff rendering: caveman emits unified diffs in tool results; render
  in a collapsible block using =diff-mode= fontification.

*Exit criteria:* a multi-turn session with code, diffs, and tool calls
renders cleanly.

** M3 — Tool-call approvals :ui:safety:
*Goal:* full permission UI parity with caveman's TUI.

caveman emits a permission-prompt event when a tool needs approval
(unless =--autopilot= is on). cavemacs must:

- Render the pending tool call inline in the buffer with the proposed
  args (file path, command, diff preview).
- Offer keyed actions: =a= approve once, =A= approve & remember (sets
  =session.tool.allow= with persistence flag), =d= deny, =e= edit args
  before approving (where caveman supports it — confirm in protocol),
  =q= dismiss / cancel turn.
- Implement as a =transient= prompt anchored to the tool-call block,
  with a fallback =read-char-choice= for non-transient users.
- Respect a customizable =cavemacs-autopilot= toggle that passes
  =--autopilot= at process start (mirrors caveman's flag exactly).

*Exit criteria:* an =Edit= tool call on a real file shows a diff,
prompts, and applies on approval.

** M4 — Session persistence & multi-session UI :sessions:
*Goal:* multiple concurrent buffers + a project-scoped session
browser.

- Each shell buffer = one caveman session = one =caveman --mode rpc=
  subprocess. Buffers named =*cavemacs: <project> · <short-id>*=.
- Persistence is *delegated to caveman* — it already writes JSONL to
  =~/.cave/agent/sessions/<cwd>/=. cavemacs reads that directory to
  enumerate.
- =M-x cavemacs= behaviour:
  - If no session in current project's repo → start a new one.
  - If exactly one live buffer for this project → pop to it.
  - If sessions exist (live or on disk) → open the session browser.
- =cavemacs-session-browser= (Phase 4 of M4):
  - Scoped to current git-repo / =project-current= root by default;
    =g a= toggles "all projects".
  - Columns: timestamp, model, turn count, last-user-prompt-preview,
    cost. Backed by =tabulated-list-mode=.
  - Actions: =RET= resume (continues subprocess against the on-disk
    session id via =session.create= with a =resumeFrom= param —
    confirm method name from RPC), =f= fork (=session.fork=), =d=
    delete on disk, =c= compact (=session.compact=).
- Continue-most-recent shortcut: =M-x cavemacs-continue= (=caveman -c=
  semantics).

*Exit criteria:* user can have three projects' sessions open in three
buffers simultaneously, kill Emacs, reopen, and resume each from the
browser.

** M5 — Slash commands, cavekit & cavemem integration :compat:
*Goal:* feature parity with caveman's TUI command surface, plus
ecosystem niceties.

- *Slash command completion.* caveman's =/= triggers built-in +
  user-defined commands (=~/.cave/commands/*.md= and =.cave/commands/
  *.md=). cavemacs:
  - Detects =/= at the start of input and pops a =completing-read=
    (or in-buffer Company/Corfu via =completion-at-point-functions=)
    with descriptions parsed from the markdown frontmatter.
  - Sources: built-ins discovered by asking caveman over RPC if it
    exposes a =commands.list= method (verify; otherwise scan the
    directories ourselves — schema is documented in
    =docs/reference/slash-commands.md=).
- *cavekit transient.* =M-x cavemacs-cavekit= opens a =transient=
  with =/ck:spec=, =/ck:build=, =/ck:check= (v4) and the v3.1.0
  command set if =SPEC.md= absent but =context/kits/= present. Each
  entry just sends the corresponding slash command into the active
  session.
- *cavemem integration.*
  - =M-x cavemacs-memory-search= → prompts for a query, sends
    =/memory search= in current session OR shells out to =cavemem
    search= directly for a non-session lookup (configurable).
  - =M-x cavemacs-memory-viewer= → =browse-url
    "http://127.0.0.1:37777"=, auto-starting the worker via =cavemem
    start= if not running.
  - =M-x cavemacs-memory-sync-from-claude= → wraps =/memory sync
    --from claude=.
- *MCP visibility (read-only).* Parse =.mcp.json= in project root +
  =~/.cave/settings.json=; show configured servers in a header-line
  segment. No management UI in v1.

*Exit criteria:* a cavekit-using project can run its full
spec → build → check loop from inside Emacs without touching the
terminal.

** M6 — Mode flags, models, providers :config:
*Goal:* expose caveman's important CLI flags as Emacs customs and a
per-shell transient.

Customs (all =defcustom= in =cavemacs=):
- =cavemacs-binary= (default ="caveman"=)
- =cavemacs-extra-args= (list of strings appended to process invocation)
- =cavemacs-default-model= → =--model=
- =cavemacs-default-provider= → =--provider=
- =cavemacs-thinking-level= → =--thinking= (=off|minimal|low|medium|high|xhigh=)
- =cavemacs-caveman-mode= → =--caveman-mode= (=lite|full|ultra=) or
  =--no-caveman-mode=
- =cavemacs-architect-model= / =cavemacs-editor-model= → split
- =cavemacs-autopilot= → =--autopilot=
- =cavemacs-plan-mode= → start session via =--plan=
- =cavemacs-goal-mode= → =--goal "…"= (autonomous loop)
- =cavemacs-environment= (alist of extra env vars for the subprocess)

Per-shell transient: =C-c C-o= in a cavemacs buffer opens a
=transient= showing the *active* flags for the current process; some
toggle live via slash commands (=/model=, =/thinking=), others
require a fresh session (warn the user).

*Exit criteria:* user can switch model/thinking-level mid-session
where caveman supports it, and start =--plan= / =--goal= sessions
from a menu.

** M7 — Tests & CI :quality:
- Unit tests for =cavemacs-rpc.el= JSONL framing (partial reads,
  multi-message reads, malformed line recovery).
- =cavemacs-mock= replays a captured transcript JSONL into a fake
  process; render-layer tests assert buffer text/properties after
  replay.
- Integration test: spawn a real =caveman --mode rpc= with a mock
  provider (caveman has =--provider mock= or similar — verify; if
  absent, skip integration in CI and run locally only).
- =makem.sh= or =eldev= for linting + byte-compile + ERT.
- GitHub Actions matrix: Emacs 30.2 on Linux + macOS.

** M8 — Documentation & release :docs:
- =README.org=: install, quickstart, key bindings, custom variables.
- =CHANGELOG.md=.
- Submit to MELPA after a stable release.

** M9 — Stretch :stretch:
- *Daemon transport* (=caveman serve= + WebSocket). Lets multiple
  Emacs frames attach to the same session, survives Emacs restart
  mid-turn. Implement as an alternate transport behind a defcustom
  =cavemacs-transport= (='subprocess= default, ='daemon= opt-in).
  Needs =websocket.el= or similar.
- *Worker dispatch.* Recognise =& prompt= prefix to dispatch to a
  registered =caveman worker= (UI parity with caveman's TUI).
- *Cavegemma model preset.* One-click defcustom set targeting the
  fine-tuned model.
- *eldoc / xref glue* for files mentioned in tool-call output —
  click-to-jump.

* Open questions to confirm during M0

These need protocol-level verification by reading caveman-code's
source and/or running the binary; assumptions in this plan are
flagged inline above.

1. Exact shape of =session.events= — long-lived subscription or
   request-per-batch? Cancellation semantics?
2. Permission-prompt event name and the =session.tool.allow= response
   schema (one-shot vs. "always" flag).
3. Resume protocol — is it =session.create {resumeFrom: id}=, a
   separate =session.resume=, or do we re-exec the binary with =-c=
   / =-r= flags and use RPC only for new sessions?
4. Does =--mode rpc= surface a =commands.list= or similar, or must
   cavemacs scan =~/.cave/commands/= itself?
5. Mid-session model/thinking switches — supported via slash command,
   RPC method, or process restart only?
6. Does caveman emit structured diff events for =Edit= tools, or only
   final tool-result text containing a diff? (Affects M3 diff UI.)

* Non-goals (v1)

- No reimplementation of caveman-mode compression in Elisp — caveman
  does it.
- No native MCP client in Emacs — caveman is the MCP host.
- No memory storage in Emacs — cavemem owns that.
- No support for arbitrary LLM providers outside what caveman already
  configures. cavemacs is a *front-end*, not an alternative agent.
- No Windows-specific work in v1 (matches caveman's own daemon
  roadmap).

* Known issues

- Resuming an existing session (session browser / -continue) does not
  restore the model in the header line. The pretty header keeps the
  default/last value until the next assistant message republishes
  meta. Likely fix: on session resume, call =get_state= (or similar)
  and push =:provider= / =:model= into =cavemacs-pretty--header-state=
  before the first turn.

** Fixed

- v0.0.14: Kill/yank on tool-card body lines used to include the
  leading =│ = box prefix and the =╭=/=╰= surrounding rules. Fixed by
  tagging every rule glyph with a =cavemacs-rule= text property and
  installing a buffer-local =filter-buffer-substring-function= in
  =cavemacs-shell-mode= that drops tagged chars from copied text.

* Timeline (rough, solo developer, part-time)

| Milestone | Effort |
|-----------+--------|
| M0        | 1–2 d  |
| M1        | 3–5 d  |
| M2        | 3–4 d  |
| M3        | 2–3 d  |
| M4        | 3–5 d  |
| M5        | 3–4 d  |
| M6        | 1–2 d  |
| M7        | 2–3 d  |
| M8        | 1–2 d  |
| *Total to v1.0* | *~3–4 weeks part-time* |
| M9 stretch | open-ended |
